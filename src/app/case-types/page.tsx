import CaseTypes from "@/components/CaseTypes";
import { Suspense } from "react";

const CaseTypesPage = () => {
  return (
    <Suspense>
      <CaseTypes />
    </Suspense>
  );
};

export default CaseTypesPage;
